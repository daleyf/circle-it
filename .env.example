# choose one provider
PROVIDER=openai
MODEL_NAME=gpt-4o-mini

# openai-style keys (only needed for cloud use)
OPENAI_API_KEY=sk-...

# behavior
HOTKEY=<ctrl>+<alt>+c
ANSWER_MAX_TOKENS=256
LOG_DIR=./logs
